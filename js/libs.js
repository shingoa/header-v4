if (typeof($) === "undefined" || !$ || typeof($.templates) === "undefined" || !$.templates)
{
	//=include vendor/jsrender.js
}

if (typeof(moment) === "undefined" || !moment)
{
	//=include vendor/moment.js
}
